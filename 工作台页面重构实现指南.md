# 工作台页面重构实现指南

根据架构设计文档，我们需要重构工作台页面，使其更加用户友好，提供清晰的任务引导和状态展示。

## 一、重构目标

1. **清晰引导**: 用户一眼就能知道下一步该做什么
2. **状态展示**: 明确显示当前项目和任务状态
3. **快速入口**: 提供常用功能的快速访问
4. **进度提示**: 显示项目完成进度

## 二、工作台页面重构代码

### 2.1 完整的Dashboard组件代码

```typescript
// frontend/src/pages/Dashboard.tsx
import { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { apiClient } from '@/utils/api'
import Layout from '@/components/Layout'

// 项目类型定义
interface Project {
  id: number
  title: string
  description: string | null
  status: 'active' | 'completed' | 'archived'
  user_id: number
  created_at: string
  updated_at: string
  documents?: Document[]
  enterprise_info?: EnterpriseInfo
}

interface Document {
  id: number
  title: string
  content: string | null
  content_type: string
  project_id: number | null
  user_id: number
  version: number
  is_template: number
  created_at: string
  updated_at: string
}

interface EnterpriseInfo {
  id: number
  user_id: number
  project_id: number | null
  enterprise_name: string
  address: string
  industry: string
  created_at: string
  updated_at: string
}

interface ProjectStats {
  active: number
  completed: number
  thisMonth: number
  total: number
}

export default function Dashboard() {
  const navigate = useNavigate()
  const [projects, setProjects] = useState<Project[]>([])
  const [projectStats, setProjectStats] = useState<ProjectStats>({
    active: 0,
    completed: 0,
    thisMonth: 0,
    total: 0
  })
  const [loading, setLoading] = useState(true)
  const [user, setUser] = useState<any>(null)

  // 获取用户信息
  useEffect(() => {
    const userData = localStorage.getItem('user')
    if (userData) {
      setUser(JSON.parse(userData))
    }
  }, [])

  // 获取项目数据
  useEffect(() => {
    fetchProjects()
  }, [])

  const fetchProjects = async () => {
    try {
      setLoading(true)
      const response = await apiClient.get('/projects/?page_size=10')
      const projectsData = response.data.projects || []
      setProjects(projectsData)
      
      // 计算项目统计数据
      const stats = calculateProjectStats(projectsData)
      setProjectStats(stats)
    } catch (error) {
      console.error('获取项目列表失败:', error)
    } finally {
      setLoading(false)
    }
  }

  // 计算项目统计数据
  const calculateProjectStats = (projects: Project[]): ProjectStats => {
    const now = new Date()
    const currentMonth = now.getMonth()
    const currentYear = now.getFullYear()
    
    return projects.reduce((stats, project) => {
      stats.total++
      
      if (project.status === 'active') {
        stats.active++
      } else if (project.status === 'completed') {
        stats.completed++
      }
      
      const createdDate = new Date(project.created_at)
      if (createdDate.getMonth() === currentMonth && createdDate.getFullYear() === currentYear) {
        stats.thisMonth++
      }
      
      return stats
    }, { active: 0, completed: 0, thisMonth: 0, total: 0 })
  }

  // 计算项目进度
  const calculateProjectProgress = (project: Project): number => {
    let progress = 0
    
    // 企业信息填写 (25%)
    if (project.enterprise_info) {
      progress += 25
    }
    
    // 文档创建 (50%)
    if (project.documents && project.documents.length > 0) {
      progress += 50
    }
    
    // 文档内容 (25%)
    if (project.documents && project.documents.length > 0) {
      const hasContent = project.documents.some(doc => doc.content && doc.content.length > 0)
      if (hasContent) {
        progress += 25
      }
    }
    
    return Math.min(progress, 100)
  }

  // 获取最近项目
  const getRecentProjects = (): Project[] => {
    return projects
      .sort((a, b) => new Date(b.updated_at).getTime() - new Date(a.updated_at).getTime())
      .slice(0, 3)
  }

  // 获取进行中的项目
  const getActiveProjects = (): Project[] => {
    return projects.filter(project => project.status === 'active')
  }

  // 获取项目状态标签
  const getStatusBadge = (status: string) => {
    const badges = {
      active: 'bg-green-100 text-green-800',
      completed: 'bg-blue-100 text-blue-800',
      archived: 'bg-gray-100 text-gray-800'
    }
    const labels = {
      active: '进行中',
      completed: '已完成',
      archived: '已归档'
    }
    return (
      <span className={`px-2 py-1 rounded-full text-xs font-semibold ${badges[status as keyof typeof badges]}`}>
        {labels[status as keyof typeof labels]}
      </span>
    )
  }

  // 获取进度条颜色
  const getProgressColor = (progress: number): string => {
    if (progress < 30) return 'bg-red-500'
    if (progress < 70) return 'bg-yellow-500'
    return 'bg-green-500'
  }

  if (loading) {
    return (
      <Layout title="工作台">
        <div className="flex items-center justify-center h-64">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary"></div>
        </div>
      </Layout>
    )
  }

  return (
    <Layout title="工作台">
      <div className="space-y-6">
        {/* 欢迎区域 */}
        <div className="bg-gradient-to-r from-primary to-green-600 rounded-lg p-6 text-white">
          <h1 className="text-2xl font-bold mb-2">👋 欢迎，{user?.name || '用户'}</h1>
          <p className="text-green-100">欢迎使用悦恩应急预案人机共写平台，让预案编制更简单高效</p>
        </div>

        {/* 项目概览 */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
          <div className="bg-white p-6 rounded-lg shadow-md">
            <div className="flex items-center">
              <div className="p-3 rounded-full bg-green-100 text-green-600 mr-4">
                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div>
                <p className="text-sm text-gray-600">进行中</p>
                <p className="text-2xl font-bold text-gray-900">{projectStats.active}</p>
              </div>
            </div>
          </div>
          
          <div className="bg-white p-6 rounded-lg shadow-md">
            <div className="flex items-center">
              <div className="p-3 rounded-full bg-blue-100 text-blue-600 mr-4">
                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div>
                <p className="text-sm text-gray-600">已完成</p>
                <p className="text-2xl font-bold text-gray-900">{projectStats.completed}</p>
              </div>
            </div>
          </div>
          
          <div className="bg-white p-6 rounded-lg shadow-md">
            <div className="flex items-center">
              <div className="p-3 rounded-full bg-purple-100 text-purple-600 mr-4">
                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div>
                <p className="text-sm text-gray-600">本月创建</p>
                <p className="text-2xl font-bold text-gray-900">{projectStats.thisMonth}</p>
              </div>
            </div>
          </div>
          
          <div className="bg-white p-6 rounded-lg shadow-md">
            <div className="flex items-center">
              <div className="p-3 rounded-full bg-gray-100 text-gray-600 mr-4">
                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z" />
                </svg>
              </div>
              <div>
                <p className="text-sm text-gray-600">总计</p>
                <p className="text-2xl font-bold text-gray-900">{projectStats.total}</p>
              </div>
            </div>
          </div>
        </div>

        {/* 快速开始 */}
        <div className="bg-white rounded-lg shadow-md p-6">
          <h2 className="text-xl font-semibold text-gray-900 mb-4">🚀 快速开始</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <button
              onClick={() => navigate('/projects?action=create')}
              className="p-4 border-2 border-dashed border-gray-300 rounded-lg hover:border-primary hover:bg-green-50 transition-colors text-left"
            >
              <div className="flex items-center mb-2">
                <span className="text-2xl mr-2">🆕</span>
                <span className="font-medium text-gray-900">创建新项目</span>
              </div>
              <p className="text-sm text-gray-600">从零开始创建一个新的应急预案项目</p>
            </button>
            
            <button
              onClick={() => {
                const activeProject = getActiveProjects()[0]
                if (activeProject) {
                  navigate(`/projects/${activeProject.id}/edit`)
                } else {
                  navigate('/projects?action=create')
                }
              }}
              className="p-4 border-2 border-dashed border-gray-300 rounded-lg hover:border-primary hover:bg-green-50 transition-colors text-left"
            >
              <div className="flex items-center mb-2">
                <span className="text-2xl mr-2">📋</span>
                <span className="font-medium text-gray-900">继续编辑</span>
              </div>
              <p className="text-sm text-gray-600">
                {getActiveProjects().length > 0 
                  ? `继续编辑: ${getActiveProjects()[0].title}` 
                  : '暂无进行中的项目'
                }
              </p>
            </button>
            
            <button
              onClick={() => navigate('/templates')}
              className="p-4 border-2 border-dashed border-gray-300 rounded-lg hover:border-primary hover:bg-green-50 transition-colors text-left"
            >
              <div className="flex items-center mb-2">
                <span className="text-2xl mr-2">📂</span>
                <span className="font-medium text-gray-900">使用模板</span>
              </div>
              <p className="text-sm text-gray-600">基于模板快速创建预案</p>
            </button>
          </div>
        </div>

        {/* 最近项目 */}
        <div className="bg-white rounded-lg shadow-md p-6">
          <div className="flex justify-between items-center mb-4">
            <h2 className="text-xl font-semibold text-gray-900">📈 最近项目</h2>
            <button
              onClick={() => navigate('/projects')}
              className="text-primary hover:text-green-700 text-sm font-medium"
            >
              查看全部 →
            </button>
          </div>
          
          {getRecentProjects().length === 0 ? (
            <div className="text-center py-8">
              <p className="text-gray-500 mb-4">暂无项目，点击上方按钮创建您的第一个项目</p>
              <button
                onClick={() => navigate('/projects?action=create')}
                className="bg-primary text-white px-6 py-2 rounded-md hover:bg-green-700 transition-colors"
              >
                创建项目
              </button>
            </div>
          ) : (
            <div className="space-y-4">
              {getRecentProjects().map((project) => {
                const progress = calculateProjectProgress(project)
                return (
                  <div key={project.id} className="border border-gray-200 rounded-lg p-4">
                    <div className="flex justify-between items-start mb-2">
                      <div className="flex-1">
                        <h3 className="text-lg font-medium text-gray-900">{project.title}</h3>
                        <p className="text-sm text-gray-600 mt-1">
                          {project.description || '暂无描述'}
                        </p>
                      </div>
                      {getStatusBadge(project.status)}
                    </div>
                    
                    {/* 进度条 */}
                    <div className="mt-3">
                      <div className="flex justify-between items-center mb-1">
                        <span className="text-sm text-gray-600">完成进度</span>
                        <span className="text-sm font-medium text-gray-900">{progress}%</span>
                      </div>
                      <div className="w-full bg-gray-200 rounded-full h-2">
                        <div
                          className={`h-2 rounded-full ${getProgressColor(progress)}`}
                          style={{ width: `${progress}%` }}
                        ></div>
                      </div>
                    </div>
                    
                    {/* 操作按钮 */}
                    <div className="flex gap-2 mt-4">
                      <button
                        onClick={() => navigate(`/projects/${project.id}/edit`)}
                        className="flex-1 bg-primary text-white px-4 py-2 rounded-md hover:bg-green-700 transition-colors text-sm"
                      >
                        编辑
                      </button>
                      {project.status === 'active' && (
                        <button
                          onClick={() => navigate(`/projects/${project.id}/continue`)}
                          className="flex-1 bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 transition-colors text-sm"
                        >
                          继续
                        </button>
                      )}
                      {project.status === 'completed' && (
                        <button
                          onClick={() => navigate(`/projects/${project.id}/export`)}
                          className="flex-1 bg-gray-600 text-white px-4 py-2 rounded-md hover:bg-gray-700 transition-colors text-sm"
                        >
                          导出
                        </button>
                      )}
                    </div>
                  </div>
                )
              })}
            </div>
          )}
        </div>

        {/* 学习资源 */}
        <div className="bg-white rounded-lg shadow-md p-6">
          <h2 className="text-xl font-semibold text-gray-900 mb-4">📚 学习资源</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <a
              href="/help/guide"
              className="flex items-center p-4 border border-gray-200 rounded-lg hover:border-primary hover:bg-green-50 transition-colors"
            >
              <span className="text-2xl mr-3">📖</span>
              <div>
                <h3 className="font-medium text-gray-900">使用指南</h3>
                <p className="text-sm text-gray-600">了解平台使用方法</p>
              </div>
            </a>
            
            <a
              href="/help/videos"
              className="flex items-center p-4 border border-gray-200 rounded-lg hover:border-primary hover:bg-green-50 transition-colors"
            >
              <span className="text-2xl mr-3">🎥</span>
              <div>
                <h3 className="font-medium text-gray-900">视频教程</h3>
                <p className="text-sm text-gray-600">观看操作演示视频</p>
              </div>
            </a>
            
            <a
              href="/help/faq"
              className="flex items-center p-4 border border-gray-200 rounded-lg hover:border-primary hover:bg-green-50 transition-colors"
            >
              <span className="text-2xl mr-3">❓</span>
              <div>
                <h3 className="font-medium text-gray-900">常见问题</h3>
                <p className="text-sm text-gray-600">查看常见问题解答</p>
              </div>
            </a>
          </div>
        </div>
      </div>
    </Layout>
  )
}
```

### 2.2 路由更新

需要更新路由配置，支持新的项目操作：

```typescript
// frontend/src/App.tsx (部分更新)
// 在Routes组件中添加以下路由

<Route path="/projects?action=create" element={
  <RouteGuard requireAuth={true}>
    <ProtectedRoute>
      <Projects createMode={true} />
    </ProtectedRoute>
  </RouteGuard>
} />

<Route path="/projects/:id/edit" element={
  <RouteGuard requireAuth={true}>
    <ProtectedRoute>
      <Projects editMode={true} />
    </ProtectedRoute>
  </RouteGuard>
} />

<Route path="/projects/:id/continue" element={
  <RouteGuard requireAuth={true}>
    <ProtectedRoute>
      <Projects continueMode={true} />
    </ProtectedRoute>
  </RouteGuard>
} />

<Route path="/projects/:id/export" element={
  <RouteGuard requireAuth={true}>
    <ProtectedRoute>
      <Projects exportMode={true} />
    </ProtectedRoute>
  </RouteGuard>
} />
```

### 2.3 后端API增强

需要添加项目统计API：

```python
# backend/app/routes/projects.py (添加以下路由)

@router.get("/stats", response_model=dict)
async def get_project_stats(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    """
    获取用户项目统计信息
    
    Returns:
        项目统计数据
    """
    try:
        # 获取所有项目
        projects = db.query(Project).filter(Project.user_id == current_user.id).all()
        
        # 计算统计数据
        now = datetime.utcnow()
        current_month = now.month
        current_year = now.year
        
        stats = {
            "active": 0,
            "completed": 0,
            "archived": 0,
            "this_month": 0,
            "total": len(projects)
        }
        
        for project in projects:
            # 状态统计
            if project.status == "active":
                stats["active"] += 1
            elif project.status == "completed":
                stats["completed"] += 1
            elif project.status == "archived":
                stats["archived"] += 1
            
            # 本月创建统计
            if project.created_at.month == current_month and project.created_at.year == current_year:
                stats["this_month"] += 1
        
        return stats
        
    except Exception as e:
        logger.error(f"获取项目统计失败: {e}")
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail="获取项目统计失败"
        )
```

### 2.4 项目进度计算逻辑

项目进度计算基于以下步骤：

1. **企业信息填写 (25%)**
   - 检查项目是否有关联的企业信息
   - 如果有，进度+25%

2. **文档创建 (50%)**
   - 检查项目是否有文档
   - 如果有，进度+50%

3. **文档内容 (25%)**
   - 检查文档是否有内容
   - 如果有，进度+25%

## 三、左侧菜单重构

### 3.1 更新Layout组件

```typescript
// frontend/src/components/Layout.tsx (更新navItems)

const navItems = [
  { 
    path: '/dashboard', 
    label: '工作台', 
    icon: '🏠',
    children: [
      { path: '/dashboard', label: '首页' },
      { path: '/projects', label: '项目管理' },
    ]
  },
  { 
    path: '/projects', 
    label: '项目管理', 
    icon: '📁',
    children: [
      { path: '/projects', label: '我的项目' },
      { path: '/templates', label: '项目模板' },
    ]
  },
  { 
    path: '/data-center', 
    label: '数据中心', 
    icon: '📊',
    children: [
      { path: '/enterprise-info', label: '企业信息库' },
      { path: '/templates', label: '预案模板库' },
    ]
  },
]

// 添加条件渲染的预案编制菜单
const getProjectSpecificNavItems = (projectId?: number) => {
  if (!projectId) return []
  
  return [
    {
      path: `/projects/${projectId}/enterprise-info`,
      label: '企业信息',
      icon: '📋',
      children: [
        { path: `/projects/${projectId}/enterprise-info`, label: '基本信息' },
        { path: `/projects/${projectId}/enterprise-info?step=production`, label: '生产过程' },
        { path: `/projects/${projectId}/enterprise-info?step=environment`, label: '环境信息' },
        { path: `/projects/${projectId}/enterprise-info?step=permits`, label: '环保手续' },
      ]
    },
    {
      path: `/projects/${projectId}/ai-generate`,
      label: 'AI生成',
      icon: '🤖',
      children: [
        { path: `/projects/${projectId}/ai-generate`, label: '选择模板' },
        { path: `/projects/${projectId}/ai-generate/history`, label: '生成历史' },
      ]
    },
    {
      path: `/projects/${projectId}/editor`,
      label: '编辑器',
      icon: '✏️',
      children: [
        { path: `/projects/${projectId}/editor`, label: '文档编辑' },
        { path: `/projects/${projectId}/editor/history`, label: '版本历史' },
      ]
    },
    {
      path: `/projects/${projectId}/export`,
      label: '导出中心',
      icon: '📤',
      children: [
        { path: `/projects/${projectId}/export`, label: 'PDF导出' },
        { path: `/projects/${projectId}/export/history`, label: '导出历史' },
      ]
    },
  ]
}
```

## 四、工作逻辑和跳转关系

### 4.1 主要跳转流程

1. **创建新项目**
   - 工作台 → 点击"创建新项目" → `/projects?action=create`
   - 项目创建页 → 填写基本信息 → 创建项目
   - 创建成功 → 跳转到企业信息填写页

2. **继续编辑**
   - 工作台 → 点击"继续编辑" → `/projects/{id}/continue`
   - 根据项目进度跳转到相应页面
   - 如果有企业信息但无文档 → 跳转到AI生成页
   - 如果有文档 → 跳转到编辑器页

3. **使用模板**
   - 工作台 → 点击"使用模板" → `/templates`
   - 选择模板 → 创建项目 → 跳转到企业信息填写页

### 4.2 项目状态管理

1. **项目创建**: 状态为 `active`
2. **企业信息填写**: 状态仍为 `active`，进度更新
3. **AI生成**: 状态仍为 `active`，进度更新
4. **文档编辑**: 状态仍为 `active`，进度更新
5. **项目完成**: 状态更新为 `completed`
6. **项目归档**: 状态更新为 `archived`

### 4.3 进度计算逻辑

```typescript
const calculateProjectProgress = (project: Project): number => {
  let progress = 0
  
  // 企业信息填写 (25%)
  if (project.enterprise_info) {
    progress += 25
  }
  
  // 文档创建 (50%)
  if (project.documents && project.documents.length > 0) {
    progress += 50
  }
  
  // 文档内容 (25%)
  if (project.documents && project.documents.length > 0) {
    const hasContent = project.documents.some(doc => doc.content && doc.content.length > 0)
    if (hasContent) {
      progress += 25
    }
  }
  
  return Math.min(progress, 100)
}
```

## 五、UI设计特点

1. **卡片式布局**: 使用卡片组织不同功能区域
2. **状态可视化**: 使用徽章和进度条显示状态
3. **色彩编码**: 使用不同颜色表示不同状态和进度
4. **响应式设计**: 适配不同屏幕尺寸
5. **微交互**: 添加悬停效果和过渡动画

## 六、实施步骤

1. **第一阶段**: 重构Dashboard组件UI
2. **第二阶段**: 实现项目统计API
3. **第三阶段**: 更新路由配置
4. **第四阶段**: 重构左侧菜单
5. **第五阶段**: 实现项目进度计算
6. **第六阶段**: 测试和优化

## 七、预期效果

1. **用户体验提升**: 用户能够快速了解系统状态和下一步操作
2. **操作效率提高**: 减少页面跳转，提供快速入口
3. **项目进度可视化**: 清晰显示项目完成状态
4. **导航结构优化**: 按业务流程组织菜单，减少认知负担

通过以上重构，工作台将成为用户进入系统后的有效引导中心，帮助用户快速了解系统状态并开始工作。