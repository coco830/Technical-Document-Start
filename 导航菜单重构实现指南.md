# 导航菜单重构实现指南

根据架构设计文档，我们需要重构左侧导航菜单，使其更加符合业务流程和用户使用习惯。

## 一、设计原则

1. **简化层级**: 最多两级菜单，避免复杂嵌套
2. **业务导向**: 按照应急预案创建流程组织菜单
3. **状态感知**: 根据项目状态显示相应菜单项
4. **功能聚合**: 相关功能集中在同一菜单项下

## 二、新菜单结构

### 2.1 菜单层级设计

```
📋 工作台
├── 🏠 首页 (工作台概览)
├── 📂 项目管理
│   ├── 创建新项目
│   ├── 我的项目
│   └── 项目模板
└── 📊 数据中心
    ├── 企业信息库
    └── 预案模板库

📝 预案编制 (仅在有活跃项目时显示)
├── 📋 企业信息
│   ├── 基本信息
│   ├── 生产过程
│   ├── 环境信息
│   └── 环保手续
├── 🤖 AI生成
│   ├── 选择模板
│   ├── 生成章节
│   └── 生成历史
├── ✏️ 编辑器
│   ├── 文档编辑
│   ├── 版本历史
│   └── 协作评论
└── 📤 导出中心
    ├── PDF导出
    ├── Word导出
    └── 导出历史

⚙️ 系统设置
├── 👤 个人资料
├── 🔧 系统配置
└── 📖 使用帮助
```

## 三、Layout组件重构代码

### 3.1 完整的Layout组件代码

```typescript
// frontend/src/components/Layout.tsx
import { ReactNode, useState, useEffect } from 'react'
import { useNavigate, useLocation, useParams } from 'react-router-dom'
import { useUserStore } from '@/store/userStore'
import { apiClient } from '@/utils/api'

interface LayoutProps {
  children: ReactNode
  title?: string
  showBackButton?: boolean
  actions?: ReactNode
}

interface NavItem {
  path: string
  label: string
  icon: string
  children?: NavItem[]
  badge?: string
  disabled?: boolean
}

interface ProjectInfo {
  id: number
  title: string
  status: string
}

export default function Layout({ children, title, showBackButton = false, actions }: LayoutProps) {
  const navigate = useNavigate()
  const location = useLocation()
  const params = useParams()
  const { user, logout } = useUserStore()
  const [currentProject, setCurrentProject] = useState<ProjectInfo | null>(null)
  const [sidebarCollapsed, setSidebarCollapsed] = useState(false)

  const handleLogout = () => {
    logout()
    navigate('/login')
  }

  const isActiveRoute = (path: string) => {
    return location.pathname === path || location.pathname.startsWith(path + '/')
  }

  // 获取当前项目信息
  useEffect(() => {
    const projectId = params.id
    if (projectId) {
      fetchProjectInfo(parseInt(projectId))
    } else {
      setCurrentProject(null)
    }
  }, [params.id])

  const fetchProjectInfo = async (projectId: number) => {
    try {
      const response = await apiClient.get(`/projects/${projectId}`)
      setCurrentProject(response.data)
    } catch (error) {
      console.error('获取项目信息失败:', error)
      setCurrentProject(null)
    }
  }

  // 基础导航项
  const baseNavItems: NavItem[] = [
    {
      path: '/dashboard',
      label: '工作台',
      icon: '📋',
      children: [
        { path: '/dashboard', label: '首页', icon: '🏠' },
        { path: '/projects', label: '项目管理', icon: '📂' },
        { path: '/data-center', label: '数据中心', icon: '📊' },
      ]
    },
    {
      path: '/projects',
      label: '项目管理',
      icon: '📁',
      children: [
        { path: '/projects?action=create', label: '创建新项目', icon: '➕' },
        { path: '/projects', label: '我的项目', icon: '📄' },
        { path: '/templates', label: '项目模板', icon: '📋' },
      ]
    },
    {
      path: '/data-center',
      label: '数据中心',
      icon: '📊',
      children: [
        { path: '/enterprise-info', label: '企业信息库', icon: '🏢' },
        { path: '/templates', label: '预案模板库', icon: '📚' },
      ]
    },
  ]

  // 项目特定导航项
  const projectNavItems: NavItem[] = currentProject ? [
    {
      path: `/projects/${currentProject.id}/enterprise-info`,
      label: '企业信息',
      icon: '📋',
      children: [
        { path: `/projects/${currentProject.id}/enterprise-info`, label: '基本信息', icon: '📝' },
        { path: `/projects/${currentProject.id}/enterprise-info?step=production`, label: '生产过程', icon: '🏭' },
        { path: `/projects/${currentProject.id}/enterprise-info?step=environment`, label: '环境信息', icon: '🌍' },
        { path: `/projects/${currentProject.id}/enterprise-info?step=permits`, label: '环保手续', icon: '📜' },
      ]
    },
    {
      path: `/projects/${currentProject.id}/ai-generate`,
      label: 'AI生成',
      icon: '🤖',
      children: [
        { path: `/projects/${currentProject.id}/ai-generate`, label: '选择模板', icon: '🎯' },
        { path: `/projects/${currentProject.id}/ai-generate/chapters`, label: '生成章节', icon: '📑' },
        { path: `/projects/${currentProject.id}/ai-generate/history`, label: '生成历史', icon: '📜' },
      ]
    },
    {
      path: `/projects/${currentProject.id}/editor`,
      label: '编辑器',
      icon: '✏️',
      children: [
        { path: `/projects/${currentProject.id}/editor`, label: '文档编辑', icon: '📝' },
        { path: `/projects/${currentProject.id}/editor/history`, label: '版本历史', icon: '🔄' },
        { path: `/projects/${currentProject.id}/editor/comments`, label: '协作评论', icon: '💬' },
      ]
    },
    {
      path: `/projects/${currentProject.id}/export`,
      label: '导出中心',
      icon: '📤',
      children: [
        { path: `/projects/${currentProject.id}/export`, label: 'PDF导出', icon: '📄' },
        { path: `/projects/${currentProject.id}/export/word`, label: 'Word导出', icon: '📝' },
        { path: `/projects/${currentProject.id}/export/history`, label: '导出历史', icon: '📚' },
      ]
    },
  ] : []

  // 系统设置导航项
  const settingsNavItems: NavItem[] = [
    {
      path: '/profile',
      label: '个人资料',
      icon: '👤'
    },
    {
      path: '/settings',
      label: '系统配置',
      icon: '🔧'
    },
    {
      path: '/help',
      label: '使用帮助',
      icon: '📖'
    },
  ]

  // 合并所有导航项
  const allNavItems = [
    ...baseNavItems,
    ...(projectNavItems.length > 0 ? [{
      path: `/projects/${currentProject?.id}`,
      label: '预案编制',
      icon: '📝',
      children: projectNavItems,
      badge: currentProject?.status === 'active' ? '进行中' : undefined
    }] : []),
    ...settingsNavItems
  ]

  // 渲染导航项
  const renderNavItem = (item: NavItem, level = 0) => {
    const isActive = isActiveRoute(item.path)
    const hasChildren = item.children && item.children.length > 0
    const isExpanded = isActive || (hasChildren && item.children.some(child => isActiveRoute(child.path)))

    return (
      <div key={item.path} className={`nav-item ${level > 0 ? 'ml-4' : ''}`}>
        <button
          onClick={() => {
            if (hasChildren) {
              // 切换子菜单展开状态
              const element = document.getElementById(`nav-${item.path}`)
              if (element) {
                element.classList.toggle('expanded')
              }
            } else {
              navigate(item.path)
            }
          }}
          className={`w-full text-left group flex items-center justify-between px-3 py-2 text-sm font-medium rounded-md transition-colors ${
            isActive
              ? 'bg-primary text-white'
              : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'
          } ${item.disabled ? 'opacity-50 cursor-not-allowed' : ''}`}
          disabled={item.disabled}
        >
          <div className="flex items-center">
            <span className="mr-3 text-lg">{item.icon}</span>
            <span>{item.label}</span>
            {item.badge && (
              <span className="ml-2 px-2 py-1 text-xs rounded-full bg-red-100 text-red-800">
                {item.badge}
              </span>
            )}
          </div>
          {hasChildren && (
            <svg
              className={`w-4 h-4 transition-transform ${isExpanded ? 'rotate-90' : ''}`}
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
            </svg>
          )}
        </button>
        
        {hasChildren && (
          <div
            id={`nav-${item.path}`}
            className={`nav-children ${isExpanded ? 'expanded' : 'collapsed'}`}
          >
            {item.children.map(child => renderNavItem(child, level + 1))}
          </div>
        )}
      </div>
    )
  }

  return (
    <div className="min-h-screen bg-gray-50 flex">
      {/* 顶部导航栏 */}
      <nav className="bg-white shadow-sm border-b fixed top-0 left-0 right-0 z-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between h-16">
            {/* 左侧 */}
            <div className="flex items-center">
              {/* 侧边栏切换按钮 */}
              <button
                onClick={() => setSidebarCollapsed(!sidebarCollapsed)}
                className="mr-4 text-gray-600 hover:text-primary transition-colors"
                title="切换侧边栏"
              >
                <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  {sidebarCollapsed ? (
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
                  ) : (
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                  )}
                </svg>
              </button>
              
              {showBackButton && (
                <button
                  onClick={() => navigate(-1)}
                  className="mr-4 text-gray-600 hover:text-primary transition-colors"
                  title="返回"
                >
                  ← 返回
                </button>
              )}
              
              <h1
                className="text-2xl font-bold text-primary cursor-pointer"
                onClick={() => navigate('/dashboard')}
              >
                🌿 悦恩人机共写平台
              </h1>
              {title && (
                <>
                  <span className="ml-4 text-gray-400">/</span>
                  <span className="ml-4 text-lg font-medium text-gray-700">{title}</span>
                </>
              )}
              
              {/* 当前项目指示器 */}
              {currentProject && (
                <div className="ml-6 px-3 py-1 bg-green-100 text-green-800 rounded-full text-sm">
                  📝 {currentProject.title}
                </div>
              )}
            </div>

            {/* 右侧 */}
            <div className="flex items-center space-x-4">
              {actions}
              <div className="flex items-center space-x-2">
                <span className="text-sm text-gray-600">
                  欢迎，{user?.name || '用户'}
                </span>
                <button
                  onClick={handleLogout}
                  className="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition-colors text-sm"
                >
                  退出登录
                </button>
              </div>
            </div>
          </div>
        </div>
      </nav>

      {/* 侧边导航栏 */}
      <aside className={`bg-white shadow-sm min-h-screen fixed left-0 top-16 z-40 transition-all duration-300 ${
        sidebarCollapsed ? 'w-16' : 'w-64'
      }`}>
        <nav className="mt-5 px-2 py-4">
          <div className="space-y-2">
            {allNavItems.map(item => renderNavItem(item))}
          </div>
        </nav>
      </aside>

      {/* 主内容区域 */}
      <main className={`flex-1 transition-all duration-300 ${
        sidebarCollapsed ? 'ml-16' : 'ml-64'
      } mt-16`}>
        <div className="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
          <div className="px-4 py-6 sm:px-0">
            {children}
          </div>
        </div>
      </main>
    </div>
  )
}
```

### 3.2 CSS样式

```css
/* frontend/src/components/Layout.css */

.nav-children {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease-in-out;
}

.nav-children.expanded {
  max-height: 500px;
}

.nav-children.collapsed {
  max-height: 0;
}

.nav-item button:hover {
  transform: translateX(2px);
}

.nav-item button:active {
  transform: translateX(1px);
}

/* 响应式设计 */
@media (max-width: 768px) {
  aside {
    transform: translateX(-100%);
  }
  
  aside:not(.collapsed) {
    transform: translateX(0);
  }
}

/* 动画效果 */
.nav-item {
  transition: all 0.2s ease;
}

.nav-item button svg {
  transition: transform 0.2s ease;
}

/* 徽章动画 */
.nav-item .badge {
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
  100% {
    opacity: 1;
  }
}
```

## 四、路由配置更新

### 4.1 新路由结构

```typescript
// frontend/src/App.tsx (路由更新)

<Routes>
  {/* 工作台路由 */}
  <Route path="/dashboard" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Dashboard />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  {/* 项目管理路由 */}
  <Route path="/projects" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Projects />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects?action=create" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <ProjectCreate />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  {/* 项目特定路由 */}
  <Route path="/projects/:id/enterprise-info" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <EnterpriseInfo />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/ai-generate" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <AIGenerate />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/ai-generate/chapters" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <AIChapters />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/ai-generate/history" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <AIHistory />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/editor" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Editor />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/editor/history" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <EditorHistory />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/editor/comments" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <EditorComments />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/export" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Export />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/export/word" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <ExportWord />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/projects/:id/export/history" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <ExportHistory />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  {/* 数据中心路由 */}
  <Route path="/data-center" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <DataCenter />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  {/* 系统设置路由 */}
  <Route path="/profile" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Profile />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/settings" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Settings />
      </ProtectedRoute>
    </RouteGuard>
  } />
  
  <Route path="/help" element={
    <RouteGuard requireAuth={true}>
      <ProtectedRoute>
        <Help />
      </ProtectedRoute>
    </RouteGuard>
  } />
</Routes>
```

## 五、状态管理优化

### 5.1 项目状态管理

```typescript
// frontend/src/store/projectStore.ts
import { create } from 'zustand'

interface Project {
  id: number
  title: string
  description: string | null
  status: 'active' | 'completed' | 'archived'
  user_id: number
  created_at: string
  updated_at: string
}

interface ProjectState {
  currentProject: Project | null
  projects: Project[]
  loading: boolean
  setCurrentProject: (project: Project | null) => void
  setProjects: (projects: Project[]) => void
  setLoading: (loading: boolean) => void
  updateProject: (project: Project) => void
}

export const useProjectStore = create<ProjectState>((set, get) => ({
  currentProject: null,
  projects: [],
  loading: false,
  
  setCurrentProject: (project) => set({ currentProject: project }),
  
  setProjects: (projects) => set({ projects }),
  
  setLoading: (loading) => set({ loading }),
  
  updateProject: (project) => {
    const { projects } = get()
    const index = projects.findIndex(p => p.id === project.id)
    if (index !== -1) {
      const updatedProjects = [...projects]
      updatedProjects[index] = project
      set({ projects: updatedProjects })
    }
  },
}))
```

## 六、实施步骤

1. **第一阶段**: 重构Layout组件基础结构
2. **第二阶段**: 实现动态菜单和项目感知
3. **第三阶段**: 更新路由配置
4. **第四阶段**: 添加状态管理
5. **第五阶段**: 实现响应式设计
6. **第六阶段**: 测试和优化

## 七、预期效果

1. **导航清晰**: 用户能够快速找到所需功能
2. **上下文感知**: 菜单根据当前项目状态动态调整
3. **操作高效**: 减少页面跳转，提供直接访问
4. **视觉友好**: 清晰的视觉层次和状态指示

通过以上重构，导航菜单将成为用户操作系统的有效引导工具，帮助用户快速定位功能并完成工作任务。